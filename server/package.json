{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "yarn run nodemon server.js",
    "prod": "node server.js",
    "prettier": "prettier --config package.json --write \"**/*.js\" --loglevel warn"
  },
  "prettier": {
    "singleQuote": true,
    "bracketSpacing": true
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.6",
    "prettier": "^1.15.2"
  },
  "dependencies": {
    "express": "^4.16.4",
    "isomorphic-fetch": "^2.2.1"
  }
}
